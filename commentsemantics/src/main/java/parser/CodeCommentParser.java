package parser;

public class CodeCommentParser 
{
	
	// parse the comment and removes commented codes in it.
	// returns comments with out code if code is less than 50% of comments
	// if commented code is more than 50% returns empty string
	public static String parseCodeComments(String comment)
	{
		return comment;
	}
	
}
